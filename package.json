{
  "name": "smart-expense-ai-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "mobile",
    "shared/*"
  ],
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "mobile:start": "npm run start --workspace=@smart-expense-ai/mobile",
    "mobile:android": "npm run android --workspace=@smart-expense-ai/mobile",
    "mobile:check": "npm run check --workspace=@smart-expense-ai/mobile"
  }
}
